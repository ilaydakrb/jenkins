# Jenkins Pipeline Örneği

Bu Jenkinsfile, pipeline mantığını örnek olarak gösterir.
Test, Build ve Deploy aşamaları sadece `echo` ile simüle edilmiştir.

**Not:** Çalıştırmak için Jenkins ortamı gerekir.
